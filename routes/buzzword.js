const express = require('express');
const router = express.Router();

let wordArr = [];
let userScore = 0;

function addBuzzWord (buzzWord, points) {
  let buzzWords = {
    buzzWord,
    points,
    heard: false
  };
}




module.exports = router;